{
  "name": "central-de-ferramentas-api",
  "version": "1.0.0",
  "private": true,
  "description": "API da Central de Ferramentas — Express + Postgres (Neon), sem front-end junto (isso fica no GitHub Pages).",
  "scripts": {
    "migrate": "node db/migrate.js",
    "seed": "node db/seed.js",
    "setup": "npm run migrate && npm run seed",
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
